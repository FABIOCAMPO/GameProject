    with open(scores_file_path,"w") as filout:

        for cle, values in dict_scores.items():
            chain_scores: str = ";".join(values)
            chain_niveau: str = str(cle) + ";" + chain_scores
            filout.write("{}\n".format(chain_niveau))
